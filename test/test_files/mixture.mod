$PROB    PHENOBARB POPULATION DATA    MIXTURE MODEL
 $DATA  pheno.dta IGNORE=@
 $INPUT  ID TIME AMT WT APGR DV
 $SUBROUTINES ADVAN1 TRANS2
 $PK
        CALLFL=1
        EST=MIXEST
        IF (MIXNUM.EQ.2) THEN
          CL=THETA(2)*THETA(1)*EXP(ETA(3))
          V=THETA(4)*THETA(3)*EXP(ETA(4))
        ELSE
          CL=THETA(1)*EXP(ETA(1))
          V=THETA(3)*EXP(ETA(2))
        ENDIF
        S1=V
		MIXPR1=THETA(5)
 $ERROR
 Y=F*EXP(EPS(1))
 $MIX
        P(1)=THETA(5)
        P(2)=1.-THETA(5)
			  NSPOP=2
 $THETA  (0,.0047)  (0,1)  (0,.99)  (0,1)  (0,.5,1)
 $OMEGA  BLOCK(2) .05  .01  .03
 $OMEGA  BLOCK(2) SAME
 $SIGMA  .02
 $ESTM   MAXEVAL=500   PRINT=5
 $TABLE ID EST MIXPR1 NOAPPEND NOPRINT ONEHEADER FILE=mixtab
